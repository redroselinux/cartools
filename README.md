# cartools

cartools is a set of utilities for Redrose Linux package maintainers.
Some of the tools are usable for any package manager.

Commands:
- `stage` - stage a build's package to a staging dir
- `mkcar` - create the `/car` [metadata file](https://docs.redroselinux.org/#/car?id=package-format) for packages
